version: "3.8"

services:
  # Optional: PostgreSQL for future use
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: newbot-postgres
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: newbot
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  # Optional: Redis for future use
  # redis:
  #   image: redis:7-alpine
  #   container_name: newbot-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data

# volumes:
#   postgres_data:
#   redis_data:

# networks:
#   default:
#     name: newbot-network
